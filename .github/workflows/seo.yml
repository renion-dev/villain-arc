name: Daily SEO & Content
on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate RSS feed
        run: python3 generate_rss.py

      - name: Generate OG image
        run: python3 generate_og.py

      - name: Generate content
        run: python3 generate_content.py

      - name: Commit all
        run: |
          git config user.name "villain-arc-bot"
          git config user.email "bot@villainarc.com"
          git add feed.xml og-image.svg content_queue.json agent_state.json
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto: Daily SEO & content $(date +%Y-%m-%d)"
          git push
